#pragma once

enum class ToolType  { Circle, Rectangle, RoundedRect, Hexagon, Line, Freehand, Text, Move, Resize, Eraser };
enum class ShapeType { Circle, Rectangle, RoundedRect, Hexagon, Line, Freehand, Text };
